name: Vulkan smoke build

on:
  push:
    branches: ["main", "master"]
  pull_request:
  workflow_dispatch:

jobs:
  linux-vulkan:
    runs-on: ubuntu-24.04
    steps:
      - name: Check out
        uses: actions/checkout@v4

      - name: Install build dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            build-essential \
            cmake \
            ninja-build \
            glslc \
            libvulkan-dev \
            spirv-headers \
            vulkan-tools

      - name: Configure
        run: cmake -B build -G Ninja -DGGML_VULKAN=ON -DLLAMA_BUILD_TESTS=OFF

      - name: Build
        run: cmake --build build --target llama-bench llama-cli -j "$(nproc)"

      - name: Archive binaries
        uses: actions/upload-artifact@v4
        with:
          name: linux-vulkan-smoke-binaries
          path: |
            build/bin/llama-bench
            build/bin/llama-cli

  android-arm64-vulkan:
    runs-on: ubuntu-24.04
    steps:
      - name: Check out
        uses: actions/checkout@v4

      - name: Set up Android SDK
        uses: android-actions/setup-android@v3
        with:
          log-accepted-android-sdk-licenses: false

      - name: Install Android NDK and build dependencies
        run: |
          sdkmanager "ndk;27.2.12479018"
          sudo apt-get update
          sudo apt-get install -y \
            cmake \
            ninja-build \
            glslc \
            zip
          echo "ANDROID_NDK_ROOT=$ANDROID_HOME/ndk/27.2.12479018" >> "$GITHUB_ENV"

      - name: Configure Android arm64 Vulkan
        run: |
          cmake -B build-android-arm64-vulkan -G Ninja \
            -DCMAKE_BUILD_TYPE=Release \
            -DCMAKE_TOOLCHAIN_FILE="$ANDROID_NDK_ROOT/build/cmake/android.toolchain.cmake" \
            -DANDROID_ABI=arm64-v8a \
            -DANDROID_PLATFORM=android-31 \
            -DANDROID_STL=c++_shared \
            -DGGML_NATIVE=OFF \
            -DGGML_CPU_ARM_ARCH=armv8.5-a+fp16+i8mm \
            -DGGML_VULKAN=ON \
            -DGGML_OPENMP=OFF \
            -DLLAMA_CURL=OFF \
            -DLLAMA_BUILD_TESTS=OFF

      - name: Build Android arm64 Vulkan tools
        run: cmake --build build-android-arm64-vulkan --target llama-bench llama-cli -j "$(nproc)"

      - name: Package Android arm64 Vulkan tools
        run: |
          mkdir -p package/android-arm64-vulkan
          cp build-android-arm64-vulkan/bin/llama-bench package/android-arm64-vulkan/
          cp build-android-arm64-vulkan/bin/llama-cli package/android-arm64-vulkan/
          cp build-android-arm64-vulkan/bin/*.so* package/android-arm64-vulkan/ || true
          cp "$ANDROID_NDK_ROOT/toolchains/llvm/prebuilt/linux-x86_64/sysroot/usr/lib/aarch64-linux-android/libc++_shared.so" package/android-arm64-vulkan/
          (cd package && zip -r android-arm64-vulkan-binaries.zip android-arm64-vulkan)

      - name: Archive Android arm64 Vulkan binaries
        uses: actions/upload-artifact@v4
        with:
          name: android-arm64-vulkan-binaries
          path: package/android-arm64-vulkan-binaries.zip
